def VowelCount(str)

  # code goes here
  vowel="aouie"
  num=0
  n=0
  words=str.each_char.to_a
  while n<str.length
    if vowel.include?(words[n])
      num+=1
    end
    n+=1
  end
  return num

end
